When given a photo of the daily Goose pet-list, extract every row from every section/table on the page. Do not skip anything or hallucinate a name — if a name is genuinely unreadable, write it exactly as "UNREADABLE" rather than guessing a plausible-sounding one. Double-check every name against the image before finalizing your answer.

Reply with a short one-line intro, then a single fenced code block labeled json containing ONLY a JSON array, structured like this:

[
  {
    "name": "Pet's name",
    "tags": ["Dog Aggressive", "Special Needs"],
    "details": [
      {"label": "Breed/Age", "value": "..."},
      {"label": "Owner Phone", "value": "..."},
      {"label": "Space", "value": "..."},
      {"label": "Departing", "value": "..."},
      {"label": "Section", "value": "..."}
    ]
  }
]

Rules:
- "tags" is ONLY short safety/attention flags actually printed in the Tags column (e.g. "Dog Aggressive", "Special Needs", "Passed DDC Eval"). Leave it as an empty array if there are none. Do not put ordinary notes here.
- "details" holds everything else as label/value pairs: breed/age/weight, owner name and phone, space or run number, departing date/time, and which section (e.g. "Gold Package - Sat, June 27, 2026") the row belongs to.
- Include every pet from every table on the page, in the order they appear.
- Do not add commentary inside the code block. Keep the reply short outside of it.

---

Alternative: if asked to "save this as a CSV file" instead, produce a downloadable CSV artifact with exactly these three columns: name, tags, details.
- name: the pet's name, plain text.
- tags: the same safety/attention flags as above, joined with a pipe character, e.g. Dog Aggressive|Fence Fighter. Leave blank if none.
- details: the same label/value pairs as above, written as "Label: Value" and joined with a semicolon, e.g. Breed/Age: Terrier Mix, 30 lbs, 2 yrs, 9 mo; Owner Phone: (602) 321-0781 (Jamison Waldrip); Space: 107-P (RF); Departing: Jun 29, 7:00am; Section: Gold Package - Sat, June 27, 2026
- Do not use literal newlines inside any cell. Quote any field containing a comma, per standard CSV rules.
